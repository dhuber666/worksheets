import React from "react"
import AppBar from "../components/AppBar";

type Props = {};
const Home = (props: Props) => {
    return (
        <div>
            <AppBar/>
        </div>
    );
};

export default Home;